# CLAUDE.md

このファイルは、このリポジトリで Claude Code が作業する際のガイドラインです。

## プロジェクト概要

task-board プロジェクト。

> TODO: プロジェクトの目的、技術スタック(フレームワーク、言語、DB など)、
> ディレクトリ構成が決まり次第、このセクションに追記してください。

## 開発コマンド

> TODO: `npm install` / `npm run dev` / `npm test` などのビルド・実行・テストコマンドが
> 決まり次第、ここに記載してください。

## Git 運用ルール

- **コードに変更を加えたら、そのたびに commit し、GitHub へ push すること。**
  - 変更をローカルに溜め込まず、意味のある単位(1つの修正・1つの機能追加など)ごとに
    こまめに commit → push を行う。
  - 作業を中断する場合も、キリの良いところで必ず push してから終える。
- commit メッセージは変更内容が分かるように具体的に書く(「fix」「update」などの曖昧な
  メッセージのみは避ける)。
- push 前に `git status` / `git diff` で変更内容を確認し、意図しないファイル
  (認証情報、`.env`、ビルド成果物など)が含まれていないかチェックする。
- リモートリポジトリ (`origin`) が未設定の場合は、push の前に GitHub 上にリポジトリを
  作成し、`git remote add origin <URL>` で紐付ける。
- force push (`git push --force` など) は行わない。必要な場合は必ずユーザーに確認する。
- 機密情報(APIキー、パスワードなど)は絶対にコミットしない。

## コーディング規約

> TODO: プロジェクト固有のコーディング規約(命名規則、フォーマッタ/リンター設定など)が
> 決まり次第、ここに記載してください。

## デプロイ情報

- 本番URL：https://task-board-67up.vercel.app
- Supabaseプロジェクト名：komoyasan's Project
